/* write a function that accepts a number of positive number N
   the function should console log a pyramid shape 
   with N levels ysung the # character. Make sure the pyramid 
   has spaces on both the left and righ hand sides
*/
const Pramid = (Pnumber) => {
  let rval = Pnumber * 2 -1 
  for (let a=0; a< Pnumber ; a++){
    let tptal = 
  }
}

Pramid(10)